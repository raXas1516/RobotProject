from flask import Flask
app = Flask(__name__)

@app.route('/<direction>/<angle>/<distance>')
def up(direction, angle, distance):
        "send variables to arduino"
        return 'connected'
  
if __name__ == '__main__':
        app.run(debug=True, host='0.0.0.0')
